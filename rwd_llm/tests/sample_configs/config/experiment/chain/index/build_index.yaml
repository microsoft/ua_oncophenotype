_target_: "llm_lib.retrieval.retrieval_utils.build_and_get_index"
# dataset key needs to be set in containing config
dataset: ${config.experiment.dataset}
embedding_model_name: "text-embedding-ada-002"
chunk_size: 1000
chunk_overlap: 100
index_output_dir: "./tmp/index"
